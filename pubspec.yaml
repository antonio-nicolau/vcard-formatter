name: vcard_formatter
description: Dart library to read and write vCard files.
version: 0.0.1
repository: https://github.com/antonio-nicolau/vcard-formatter

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
